// This component is meant to be a higher order component, and to let us use it a 
// container for other components, same as React.Fragment.

const Auxiliary = (props) => props.children;

export default Auxiliary;